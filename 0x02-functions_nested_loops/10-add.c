#include "holberton.h"
/**
 * add - function that adds two integers
 * and returns the result.
 * @i: input intiger
 * @k: input intiger
 * Return: int
 */

int add(int i, int k)
{
	int s;

	s = (i + k);
	return (s);
}
