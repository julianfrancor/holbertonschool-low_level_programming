#include "holberton.h"

/**
 * *cap_string - function that capitalizes
 * all words of a string.
 * @s: input is array
 * Return: char
 */

char *cap_string(char *s)
{
	int i;
	char *sep;

	i = 0;
	sep[] = {32, 59, 46, 33, ?, ", (, ), ;


	while (s[i] != 0)
	{
		if (s[i] >= 'a' && s[i] <= 'z' && s[i] != )
		{
			s[i] = s[i] - 32;
		}
	i++;
	}
	return (s);
}
